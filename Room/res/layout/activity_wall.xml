<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".WallActivity" >
    
    <com.alvarosantisteban.room.TaggeableImageView 
        android:id="@+id/wallImage"
        android:layout_width="wrap_content"
   		android:layout_height="wrap_content"
   		android:contentDescription="@string/wall_description"
        />
	<!-- 
    <ListView
        android:id="@+id/editTagOptionsList"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:visibility="invisible" 
        android:entries="@array/edit_tag_options">
    </ListView>
	 -->
	 <ListView 
    android:id="@+id/editTagOptionsList"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:visibility="invisible"  
    android:entries="@array/edit_tag_options"/> 

</RelativeLayout>
